# Retrieve

Use `knowledge.catalog` or `knowledge.candidates`, select a local article, then call `knowledge.materialize` or `knowledge.history`. Compose the answer in Claude and cite the returned article and source references.
