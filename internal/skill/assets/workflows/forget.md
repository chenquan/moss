# Forget and rollback

Use `source.forget.plan` or `knowledge.rollback.plan` through stdin/stdout, show impact or version diff, and wait for explicit confirmation before the generic `plan.apply`. Use `plan.undo` only for unchanged, recoverable plans.
