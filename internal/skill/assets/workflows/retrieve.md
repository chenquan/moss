# Retrieve

Use `knowledge.catalog` or `knowledge.candidates` through stdin/stdout, select a local article, then call `knowledge.materialize` or `knowledge.history`. Compose the answer in Claude and cite the returned article and source references. Read a Moss-managed path when inline content is too large.
